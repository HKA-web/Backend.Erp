<?php

namespace Modules\{{module}}\Models;

use App\Traits\BaseModel;
use App\Traits\SerializableDate;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Notifications\Notifiable;
use Laravel\Sanctum\HasApiTokens;
use Modules\{{module}}\Database\Factories\{{model}}Factory;
use Spatie\Permission\Traits\HasRoles;

class {{model}} extends Model
{
    use HasFactory, Notifiable, HasApiTokens, HasRoles, SerializableDate, BaseModel;

    protected $connection = 'pgsql';
    protected $table = '{{schema}}.{{model_lower}}';

    protected $primaryKey = '{{model_lower}}_id';
    protected $guard_name = 'api';

    public $incrementing = false;
    protected $keyType = 'string';

    protected static function newFactory()
    {
        return {{model}}Factory::new();
    }
}
